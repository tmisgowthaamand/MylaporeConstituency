# Local environment (gitignored).
#
# Local dev: leave commented out; the Vite proxy routes /api → localhost:5050.
# Local production-build sanity check: uncomment and point at your deployed
# backend.

VITE_API_URL=https://tvk-mla-support.onrender.com/api